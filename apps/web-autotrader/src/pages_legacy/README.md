This directory preserves the former Next.js pages router files (index + api/healthz).
They were superseded by the App Router under /apps/web-autotrader/app.
Safe to delete once migration is fully validated.
